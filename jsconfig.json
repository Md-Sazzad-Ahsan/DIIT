{
  "compilerOptions": {
    "baseUrl": ".", // Base directory for resolving non-relative module names
    "paths": {
      "@/*": ["./*"] // Sets up a path alias for easier imports
    }
  },
  "include": ["app", "pages", "components","utils", "data", "SessionProvider.jsx"] // Includes common directories
}
